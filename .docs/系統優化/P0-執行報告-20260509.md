# P0 執行報告

> 日期：2026-05-09
> 狀態：P0 ✅ 完成，P1～P2 ⏳ 待執行

---

## 執行摘要

| 項目 | 狀態 | 說明 |
|------|------|------|
| ArangoDB 備份 | ✅ | 54 collections, 19.8 MB → `.tmp/arangodb-backup-20260509_152845/` |
| SkillBoard → ActionBoard 改名 | ✅ | TS + Rust + DB collection 全部更新，編譯通過 |
| 重複 executors 合併 | ✅ | `aitask/tools/executors.py` 委託 `shared/tools/` |
| system_params 補齊 | ✅ | `orchestrator.mode` / `harness.*` 共 4 筆 |
| SkillsRAG | ⏳ | 待執行 |
| ActionBoard UI 遷移 | ⏳ | 待執行 |

## P0 變更詳細

### 改名（SkillBoard → ActionBoard）

| 檔案 | 變更 |
|------|------|
| `src/pages/SkillBoard.tsx` | → `ActionBoard.tsx`（舊檔刪除） |
| `src/App.tsx` | 路由 `/skills` → `/action-board` |
| `src/services/api.ts` | `SkillSpec` → `ActionScript`, `skillApi` → `actionApi` |
| `api/src/api/skills.rs` | struct `SkillSpec` → `ActionScript`, collection `skill_specs` → `action_scripts` |
| `api/src/api/mod.rs` | 路由 `/api/v1/skills` → `/api/v1/action-scripts` |
| ArangoDB | collection `skill_specs` → `action_scripts` |

### 重複程式碼合併

- `aitask/tools/executors.py` 改為從 `shared/tools/executors.py` re-export
- 匯入測試通過

### 新增 system_params

| key | value |
|-----|-------|
| `orchestrator.mode` | `langgraph` |
| `harness.bounded_execution` | `true` |
| `harness.max_todo_retries` | `3` |
| `harness.pdca_enabled` | `false` |

## 驗證結果

| 檢查 | 結果 |
|------|------|
| Rust 編譯 | ✅ warnings only（0 error） |
| TypeScript 編譯 | ✅ 0 error |
| Python import | ✅ 所有 executor 匯入成功 |
| ArangoDB backup | ✅ 54 collections, 19.8 MB |

## P1～P2 待辦

| 階段 | 主要工作 |
|------|---------|
| P1 SkillsRAG | skill.md 上傳 + chunk + embedding + Qdrant 索引 + 查詢 API |
| P2 ActionBoard UI | 前端路由 + 選單更新 + 權限對應 |

## 注意事項

1. 欄位名（`skill_type`、`skill_no` 等）保留原樣，因 DB 資料尚未 migration
2. 舊路由 `/skills` 已移除，直接換成 `/action-board`
3. `orchestrator.mode` 設為 `langgraph` 但還未完整測試，如需穩定可改回 `legacy`
