name: Build & Release Tauri App

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:
    inputs:
      draft:
        description: 'Create as draft release'
        required: false
        default: true
        type: boolean

permissions:
  contents: write

env:
  TAURI_SIGNING_PRIVATE_KEY_PASSWORD: "abc-updater-2026"

jobs:
  build-macos:
    strategy:
      fail-fast: false
      matrix:
        include:
          - target: aarch64-apple-darwin
            runner: macos-latest
            label: macOS-ARM
          - target: x86_64-apple-darwin
            runner: macos-latest
            label: macOS-Intel

    runs-on: ${{ matrix.runner }}
    name: Build (${{ matrix.label }})

    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.target }}

      - name: Rust cache
        uses: swatinem/rust-cache@v2
        with:
          workspaces: src-tauri
          key: ${{ matrix.target }}

      - name: Install dependencies
        run: npm ci

      - name: Build frontend
        run: npm run build

      - name: Build Tauri app
        env:
          TAURI_SIGNING_PRIVATE_KEY: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY }}
        run: npx tauri build --target ${{ matrix.target }}

      - name: Upload .app.tar.gz
        uses: actions/upload-artifact@v4
        with:
          name: tauri-${{ matrix.label }}-bundle
          if-no-files-found: error
          path: src-tauri/target/${{ matrix.target }}/release/bundle/macos/*.app.tar.gz

      - name: Upload .sig signature
        uses: actions/upload-artifact@v4
        with:
          name: tauri-${{ matrix.label }}-sig
          if-no-files-found: warning
          path: src-tauri/target/${{ matrix.target }}/release/bundle/macos/*.app.tar.gz.sig

  build-windows:
    runs-on: windows-latest
    name: Build (Windows-x64)

    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Rust cache
        uses: swatinem/rust-cache@v2
        with:
          workspaces: src-tauri
          key: x86_64-pc-windows-msvc

      - name: Install dependencies
        run: npm ci

      - name: Build frontend
        run: npm run build

      - name: Build Tauri app
        env:
          TAURI_SIGNING_PRIVATE_KEY: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY }}
        run: npx tauri build

      - name: Upload .exe
        uses: actions/upload-artifact@v4
        with:
          name: tauri-Windows-x64-bundle
          if-no-files-found: error
          path: src-tauri/target/release/bundle/nsis/*.exe

      - name: Upload .sig signature
        uses: actions/upload-artifact@v4
        with:
          name: tauri-Windows-x64-sig
          if-no-files-found: warning
          path: src-tauri/target/release/bundle/nsis/*.exe.sig

  release:
    needs: [build-macos, build-windows]
    runs-on: ubuntu-latest
    name: Create Release

    steps:
      - uses: actions/checkout@v4

      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts

      - name: Verify artifacts downloaded
        run: |
          echo "=== Downloaded artifacts ==="
          find artifacts -type f 2>/dev/null | sort
          COUNT=$(find artifacts -type f 2>/dev/null | wc -l)
          echo "Total files: $COUNT"

      - name: Determine version
        id: version
        run: |
          if [[ "${{ github.ref_type }}" == "tag" ]]; then
            echo "version=${{ github.ref_name }}" >> $GITHUB_OUTPUT
            echo "is_draft=false" >> $GITHUB_OUTPUT
          else
            echo "version=dev-$(date +%Y%m%d-%H%M%S)" >> $GITHUB_OUTPUT
            echo "is_draft=true" >> $GITHUB_OUTPUT
          fi

      - name: Generate latest.json for updater
        run: |
          VERSION="${{ steps.version.outputs.version }}"
          NOTES="Release ${VERSION}"
          DATE=$(date -u +"%Y-%m-%dT%H:%M:%SZ")
          REPO="https://github.com/${{ github.repository }}/releases/download/${VERSION}"

          # Find files
          MAC_ARM_TAR=$(ls artifacts/tauri-macOS-ARM-bundle/*.app.tar.gz 2>/dev/null | head -1)
          MAC_INTEL_TAR=$(ls artifacts/tauri-macOS-Intel-bundle/*.app.tar.gz 2>/dev/null | head -1)
          WIN_EXE=$(ls artifacts/tauri-Windows-x64-bundle/*.exe 2>/dev/null | head -1)

          # Read signatures
          MAC_ARM_SIG=$(cat "artifacts/tauri-macOS-ARM-sig/$(basename $MAC_ARM_TAR).sig" 2>/dev/null || echo "")
          MAC_INTEL_SIG=$(cat "artifacts/tauri-macOS-Intel-sig/$(basename $MAC_INTEL_TAR).sig" 2>/dev/null || echo "")
          WIN_SIG=$(cat "artifacts/tauri-Windows-x64-sig/$(basename $WIN_EXE).sig" 2>/dev/null || echo "")

          # macOS ARM
          if [ -f "$MAC_ARM_TAR" ]; then
            MAC_ARM_SIZE=$(stat -c%s "$MAC_ARM_TAR")
            MAC_ARM_SHA256=$(sha256sum "$MAC_ARM_TAR" | cut -d' ' -f1)
            MAC_ARM_NAME=$(basename "$MAC_ARM_TAR")
            MAC_ARM_URL="${REPO}/${MAC_ARM_NAME}"
          fi

          # macOS Intel
          if [ -f "$MAC_INTEL_TAR" ]; then
            MAC_INTEL_SIZE=$(stat -c%s "$MAC_INTEL_TAR")
            MAC_INTEL_SHA256=$(sha256sum "$MAC_INTEL_TAR" | cut -d' ' -f1)
            MAC_INTEL_NAME=$(basename "$MAC_INTEL_TAR")
            MAC_INTEL_URL="${REPO}/${MAC_INTEL_NAME}"
          fi

          # Windows EXE
          if [ -f "$WIN_EXE" ]; then
            WIN_SIZE=$(stat -c%s "$WIN_EXE")
            WIN_SHA256=$(sha256sum "$WIN_EXE" | cut -d' ' -f1)
            WIN_NAME=$(basename "$WIN_EXE")
            WIN_URL="${REPO}/${WIN_NAME}"
          fi

          # Build latest.json (Tauri 2.x format)
          cat > latest.json << 'EOF'
          {
            "version": "${VERSION#v}",
            "notes": "${NOTES}",
            "pub_date": "${DATE}",
            "platforms": {
              "darwin-aarch64": {
                "signature": "${MAC_ARM_SIG}",
                "url": "${MAC_ARM_URL}",
                "size": ${MAC_ARM_SIZE:-0},
                "checksum": "${MAC_ARM_SHA256}"
              },
              "darwin-x86_64": {
                "signature": "${MAC_INTEL_SIG}",
                "url": "${MAC_INTEL_URL}",
                "size": ${MAC_INTEL_SIZE:-0},
                "checksum": "${MAC_INTEL_SHA256}"
              },
              "windows-x86_64": {
                "signature": "${WIN_SIG}",
                "url": "${WIN_URL}",
                "size": ${WIN_SIZE:-0},
                "checksum": "${WIN_SHA256}"
              }
            }
          }
          EOF

          echo "=== latest.json ==="
          cat latest.json

      - name: Collect release files
        run: |
          mkdir -p release-files
          find artifacts -type f \( -name "*.app.tar.gz" -o -name "*.exe" \) -exec cp {} release-files/ \; 2>/dev/null || true
          find artifacts -type f \( -name "*.sig" \) -exec cp {} release-files/ \; 2>/dev/null || true
          cp latest.json release-files/
          echo "=== Release files ==="
          ls -la release-files/

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ steps.version.outputs.version }}
          name: ABC Desktop ${{ steps.version.outputs.version }}
          draft: ${{ steps.version.outputs.is_draft == 'true' || github.event.inputs.draft == 'true' }}
          prerelease: false
          generate_release_notes: true
          files: release-files/*
